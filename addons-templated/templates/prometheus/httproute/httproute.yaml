{{- if and .Values.prometheus.enabled ((.Values.prometheus.httproute).enabled) }}
{{- $app              := .Values.prometheus }}
{{- $gatewayName      := $app.httproute.gatewayName      | default "cilium-gateway" }}
{{- $gatewayNamespace := $app.httproute.gatewayNamespace | default "kube-system" }}
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: prometheus
  namespace: {{ $app.namespace }}
spec:
  hostnames:
    - {{ $app.httproute.host }}
  parentRefs:
    - name: {{ $gatewayName }}
      namespace: {{ $gatewayNamespace }}
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: kube-prometheus-stack-prometheus
          port: 9090
{{- end }}
