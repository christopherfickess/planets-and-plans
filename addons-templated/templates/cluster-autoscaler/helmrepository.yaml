{{/* kube-system already exists — no namespace.yaml needed */}}
{{- if .Values.clusterAutoscaler.enabled }}
{{- include "flux-addons.helmrepository" (dict "repo" .Values.clusterAutoscaler.chart.repo "global" .Values.global) }}
{{- end }}
