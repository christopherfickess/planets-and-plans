# addons/clusters/azure/dev-chris/apps/keycloak/keycloak/kustomization.yaml

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: keycloak

resources:
  - release.yaml
  - service.yaml

# Removed: helmrepositories.yaml — no longer using Helm for Keycloak; operator handles deployment
# Removed: configMapGenerator for keycloak-values — values.yaml replaced by Keycloak CR spec
