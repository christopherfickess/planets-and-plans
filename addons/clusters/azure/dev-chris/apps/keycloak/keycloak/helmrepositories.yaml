# addons/clusters/azure/dev-chris/apps/keycloak/keycloak/helmrepositories.yaml
# No longer referenced by kustomization.yaml — operator replaces the Helm approach.

# Previous (OCIRepository — not a valid sourceRef kind for HelmRelease):
# apiVersion: source.toolkit.fluxcd.io/v1beta2
# kind: OCIRepository
# metadata:
#   name: bitnami-keycloak
# spec:
#   interval: 1h
#   url: oci://registry-1.docker.io/bitnamicharts/keycloak
#   ref:
#     semver: ">=24.0.0 <25.0.0"

# Previous (Bitnami OCI HelmRepository — abandoned: images not on Docker Hub, oci.bitnami.com NXDOMAIN):
# apiVersion: source.toolkit.fluxcd.io/v1beta2
# kind: HelmRepository
# metadata:
#   name: bitnami-keycloak
# spec:
#   type: oci
#   interval: 1h
#   url: oci://registry-1.docker.io/bitnamicharts
